"use strict";

import React, { PropTypes } from 'react';

class Invoice extends React.Component {
    render () {
        return (
            <div>Invoice</div>
        );
    }
}

export default Invoice;
